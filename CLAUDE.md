# Aluminum — project instructions

- Always commit and push at the end of your work, without being asked.
- `app_plan.md` is the spec of record and tracks shipped code: backport every
  code change into its snippets / as-built notes in the same session.
