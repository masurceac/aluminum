# electron-builder copies production node_modules into the unpacked app dir
# (asar: false). With pnpm's default symlinked layout it copies uiohook-napi as
# an empty directory, so the packaged app has no .node prebuild and the global
# hotkey dies. A hoisted, npm-shaped node_modules makes the copy work.
node-linker=hoisted
